import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  area:{
        marginTop: 40,
    },

  input:{
    height: 45,
    borderWidth: 1,
    borderColor: '#222',
    margin: 10,
    fontSize: 20,
    padding: 10,
    },

  resultado:{
    marginTop: 15,
    fontSize: 20,
    color: '#70163C',
    textAlign: 'center',
    fontWeight: "700",
  }
   
})

export {styles}