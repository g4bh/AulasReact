import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({

  base:{
    fontFamily: "sans-serif",
    fontSize: 25,
    color: 'white',
    marginTop: 10,
    textAlign: 'center',
    marginRight: 20,
    marginLeft: 20
  },

})

export {styles}